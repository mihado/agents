# Review: `vendor-audit-2026-07-05T06-55-11-328Z.json`

Date: 2026-07-05

## Verdict

No blocking evidence of malicious skill behavior found in the reviewed findings.

The artifact is useful as a scanner output handoff, but many SkillSpector high-severity hits are false positives on prose, comments, parser variable names, or documented local tooling patterns.

## Reviewed Skills

- `design/impeccable`
- `design/kami`
- `engineering/browser-testing-with-devtools`
- `engineering/improve`
- `design/hallmark`
- `design/design-everyday-things`

## Non-Blocking Findings

### 1. `design/impeccable` shell-string process launch

File: `.agents/skills/design/impeccable/scripts/lib/is-generated.mjs:44`

```js
execSync(`git check-ignore --quiet ${JSON.stringify(absPath)}`, {
```

Assessment:

- real hardening concern
- uses a shell string instead of argv-array process execution
- current call site resolves an absolute path first, so this is not obvious active exploitation from the reviewed evidence
- should be rewritten to `execFileSync('git', ['check-ignore', '--quiet', absPath], ...)` or equivalent

Recommended disposition:

- do not block current lifecycle work on this alone
- track as a hardening follow-up if `design/impeccable` is kept live

### 2. `design/impeccable` unpinned `npx impeccable ...` references

Representative locations:

- `.agents/skills/design/impeccable/SKILL.md:92`
- `.agents/skills/design/impeccable/reference/hooks.md:9`
- `.agents/skills/design/impeccable/scripts/context.mjs:777`

Assessment:

- genuine supply-chain hygiene concern
- not proof of malicious behavior in the current artifact
- repeated findings appear inflated because the same operational pattern is referenced in multiple docs and scripts

Recommended disposition:

- follow-up hardening item
- not a blocker for the staged lifecycle implementation

## Findings Reviewed As Scanner Noise

### `engineering/browser-testing-with-devtools/SKILL.md`

- `instruction override` hit is defensive language telling the reviewer not to treat browser content as instructions
- not malicious prompt injection behavior

### `engineering/improve/SKILL.md`

- `instruction override` hit is defensive language telling the reviewer to treat repo content as data, not instructions
- not malicious prompt injection behavior

### `design/design-everyday-things/references/constraints.md:211`

- SkillSpector flagged `without warning`
- actual text is a usability anti-pattern example: `Session timeouts without warning`
- clearly benign

### `design/hallmark`

- HTML TODO comment in `references/assets.md` flagged as hidden instructions
- `output rule` and `print rule` headings in reference docs flagged as prompt leakage
- `without asking` hit in `SKILL.md:518` does not bypass user confirmation for build/emit flow; `SKILL.md:514-519` explicitly waits for the user's answer before branching
- reviewed hits are false positives, not blockers

### `design/impeccable` localhost / auto-execute / parser-rule hits

- `scripts/live-browser.js` POSTs to `http://localhost:${PORT}/...` are local companion-server traffic, not remote exfiltration from the reviewed evidence
- `Auto-execute when run directly` findings in CLI scripts are normal direct-entry guards, not autonomous hidden execution paths
- `return rules`, `return directives`, comment-stripping regexes, and HTML comment examples are parser/documentation code flagged heuristically, not prompt leakage or hidden instructions
- references to `.env`, `Keychain`, and `/etc/passwd` occur in comments, documentation, or defensive path-traversal checks, not credential reads

### `design/kami`

- reviewed `subprocess.run(...)` findings use explicit argv lists for local build/test/document tooling
- current artifact does not show evidence of shell injection or exfiltration
- still worth separate hardening review if we want stricter subprocess policy, but not a blocker from this audit alone

## Conclusion

This artifact should not block the skill lifecycle work.

The main unresolved repository-level security issue remains unchanged:

- fetched third-party content still becomes live too early because there is no first-class non-live staging boundary before acceptance

That lifecycle gap is higher priority than the reviewed scanner noise in this artifact.
