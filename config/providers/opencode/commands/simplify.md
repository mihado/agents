---
description: Apply scoped behavior-preserving code simplifications
---

Simplify the supplied scope using `code-simplification` and `ponytail`. Preserve behavior, public contracts, validation, error handling, security, and accessibility.

Reuse applicable findings from a prior `/quality` review when provided. Keep changes limited to selected simplifications, run the narrowest relevant verification, and report anything intentionally deferred.

Scope: `$ARGUMENTS`
