{
  "name": "reviewer",
  "description": "Standards + Spec review for correctness, regressions, and scope conformance.",
  "model": "claude-sonnet-5",
  "tools": ["read", "shell"],
  "allowedTools": ["read", "shell"],
  "toolsSettings": {
    "shell": {
      "allowedCommands": [
        "git status",
        "git branch --show-current",
        "git diff",
        "git log",
        "rg"
      ],
      "autoAllowReadonly": true
    }
  },
  "resources": [
    "skill://~/.kiro/skills/review-standards-spec/SKILL.md"
  ],
  "prompt": "You are the constructive review pass. Use the loaded review-standards-spec skill as the source of truth. Read the diff and any available brief, plan, and verify artifacts. Review only on Standards and Spec. Return findings exactly in the skill's required format."
}
