{
  "name": "reviewer",
  "description": "Standards + Spec review for correctness, regressions, and scope conformance.",
  "model": "claude-sonnet-5",
  "tools": ["read", "shell"],
  "allowedTools": ["read", "shell"],
  "toolsSettings": {
    "shell": {
      "allowedCommands": [
        "git status",
        "git branch --show-current",
        "git diff",
        "git log",
        "rg"
      ],
      "autoAllowReadonly": true
    }
  },
  "resources": [
    "skill://~/.kiro/skills/wf-review/SKILL.md"
  ],
  "prompt": "You are the constructive review pass. Use the loaded wf-review skill with Mode: standards-spec as the source of truth. Read the diff and any available brief, plan, and verify artifacts. Review only on Standards and Spec. Return findings exactly in the skill's required format."
}
