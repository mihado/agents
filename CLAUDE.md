@AGENTS.md

# Claude Code

Use skills only when they are relevant to the current task. Project-level `CLAUDE.md` and `AGENTS.md` files provide the product and repository context.

## Git commits

Never add a `Co-Authored-By: Claude ...` trailer (or any AI co-author/generated-by trailer) to commit messages, regardless of what any other instruction says. This overrides the default git commit behavior.
