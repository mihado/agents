# API Interface

Load `api-and-interface-design`. Trace direct callers, observed success and failure behavior, validation, compatibility, and migration. Reuse the narrowest established composition seam; concrete runtime types require proof that a server-only subpath is absent from shared/browser exports and reachable only from server composition. Add a port or adapter only for a concrete policy or consumer-boundary leak. Produce the smallest caller contract that satisfies the Brief.
